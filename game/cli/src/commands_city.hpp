#pragma once

namespace inf::cli {

// T0021: the city golden — the showcase scene's geometry hashed for two
// seeds (game/tests/goldens/hash-city.txt).
int cmd_hash_city();

}  // namespace inf::cli
