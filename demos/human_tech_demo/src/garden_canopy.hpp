#pragma once
#include "scene.hpp"

namespace cb {
// Reproducible, metre-scale hero plants. Leaf sizes are authored explicitly;
// broad crowns arise from real branching rather than stretched source leaves.
void add_garden_hero_plants(Scene& scene);
}  // namespace cb
