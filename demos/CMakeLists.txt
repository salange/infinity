# Standalone demos: separate binaries that explore one problem outside the
# game's pipeline (renderers, generators). They may link engine libraries
# but the game never depends on them.
add_subdirectory(cityblock)
